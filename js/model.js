//функция принимает введеные данные в поле и передает в массив

function getArrElem(elem, index) {
    let arr = [];
    let i = domElem.getArrIndex.value;
    arr[i] = domElem.getArrElem.value;
    return arr;
}